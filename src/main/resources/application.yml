server:
  port: 1112
# reids相关配置
spring:
  redis:
    host: 39.102.61.252
    port: 6379
    database: 0
    password:
    lettuce:
      pool:
        #连接池最大连接数为8，负数表示没有限制
        max-active: 8
        #连接池最大阻塞等待时间
        max-wait: -1
        #连接池中最大空闲连接数，默认为8
        max-idle: 8
        #连接池中最小空闲连接数，默认为0
        min-idle: 0

